generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Transaction {
  id        String   @id @default(cuid())
  name      String
  amount    Float
  type      String
  category  String
  date      DateTime
  createdAt DateTime @default(now())
}
